box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.17
        theme: ghostwriter
        flags: --buildDrafts=true
deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: m0t0k1ch1st0ry.com
        basedir: public
