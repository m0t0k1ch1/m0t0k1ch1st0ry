box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.18"
        config: config.toml
        disable_pygments: true
        flags: -v --buildDrafts=true
deploy:
  steps:
    - install-packages:
        packages: git ssh-client
    - lukevivier/gh-pages:
        token: $GIT_TOKEN
        domain: m0t0k1ch1st0ry.com
        basedir: public
