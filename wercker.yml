box: debian
build:
  steps:
    - arjen/hugo-build:
        version: 0.14
        theme: vienna
        flags: --buildDrafts=true
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: m0t0k1ch1st0ry.com
        basedir: public